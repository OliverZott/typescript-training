var isWinter: boolean = true;

var count: number = 123;


var names: string[] = ["Sigi", "Olli", "Leni"];

function getNames(): void {
    names.forEach(element => {
        console.log(element);
    });
}


getNames();